# JWT-Authentication-and-Deployment
This project takes you on a journey to learn user authentication (using JWT). You will dive deep into deploying frontend and backend code, mastering NGINX and reverse proxies, and exploring many other crucial concepts that developers often struggle to find in one place
